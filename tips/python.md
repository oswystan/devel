# virtual env

```bash
## install uv for local virtual environment setup
pip install uv

## create a new project 
mkdir dev && cd dev
uv init .
uv venv
source .venv/bin/activate

## add new deps
uv add require

```

